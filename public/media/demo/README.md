# Authorized demo media

Openly licensed demonstration clips live here (`clip-01.mp4`, `clip-02.mp4`). Case catalog entries wire them through `videoSrc` / `posterSrc` in `data/cases.ts` (via `data/assign-media.ts`).

Replace these with team-recorded or permission-cleared footage when you move beyond the prototype. Posts without a real clip are text-only or image posts — the feed no longer uses SVG placeholders for media.
