# Authorized demo media

Place team-recorded, permission-cleared, or openly licensed demonstration clips in this directory, then set the corresponding `videoSrc`/`posterSrc` fields in `data/cases.ts`.

The committed prototype intentionally ships without third-party sports footage. When a clip is absent, the UI displays “Authorized demo clip placeholder” together with a textual incident description.
